# Dot-matrix recorder

A floating record/screenshot control. Every icon is a 5×5 grid of dots. At rest it is a small
vertical pill. When you hover it, it opens into a menu. Record counts down 3, 2, 1 on the red
lens. Then the pill folds into a timer while a band of light sweeps across the lens.

Standalone:

```bash
./gradlew :dot-matrix-recorder:run
```

Or open it from the [showcase](../README.md).

## Recording

Auto-generated by Spectre:

https://github.com/rock3r/bioparco/releases/download/recordings/dot-matrix-recorder.mp4

Regenerate with `./gradlew :recordings:recordSpecimens`. See [docs/RECORDING.md](../docs/RECORDING.md).

## How to use it

| State | Collapsed | Hovered |
|---|---|---|
| Idle | Vertical pill: lens and screenshot frame | Record, Screenshot |
| Counting down | Horizontal pill: digit and a grey `00:00` | Grey `00:00` (click to cancel), Screenshot |
| Recording | Horizontal pill: sweeping lens and timer | Timer (click to stop), Restart, Delete |

Stop and Delete both send the pill back to idle. Nothing is actually captured: the
specimen is the control, not a screen recorder.

## Concepts

- `RecorderState` is a plain reducer. It takes the time as a parameter, so the countdown and
  the timer are unit tested without a clock.
- The countdown ends on its own boundary. A late frame does not steal time from the recording.
- `DotMatrix` eases each of the 25 dots on its own towards a target level. Glyph changes
  dissolve dot by dot instead of cutting.
- The dot frame loop writes only a draw-phase tick. The timer text recomposes once per second.
- One `AnimatedContent` morphs between four faces (dock, menu, recording menu, pill). A
  `SizeTransform` spring moves the outline, and each face fades in with a short blur.
- A second `AnimatedContent`, keyed on a session counter, blurs the whole pill out on Stop
  or Delete and brings the idle pill back.

## Reference

- Sasha Birukoff's Halogen teaser: https://x.com/sashabirukoff/status/2103156002220589129

This is a rebuild from the video. It is not the original code or assets.

## Tricky bits

- Draw the outline and fill on a `Box` that wraps the `AnimatedContent`, not on the
  `AnimatedContent` modifier itself. On the modifier, the outline disappeared whenever a dot
  redrew while no size animation was running (seen in `ImageComposeScene` renders).
- Each face gets a frozen snapshot of the state (`Look`). Without it, the face that is leaving
  would show the new state while it fades out.
- Collapsing waits 220 ms after the pointer leaves. Without the wait, the pill can flicker when
  the outline shrinks under a pointer that has not moved.
- The record lens hides its four corner dots, so it reads as round. The countdown digits are
  drawn inside that same 21-dot disc.
